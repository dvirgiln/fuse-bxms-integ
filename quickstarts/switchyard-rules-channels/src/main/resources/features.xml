<?xml version="1.0" encoding="UTF-8"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.0.0">
	<repository>mvn:org.switchyard.karaf/switchyard/${switchyard.version}/xml/features</repository>
	<feature name="switchyard-drools-channels" version="0.0.1-SNAPSHOT" resolver="(obr)">
		<feature version="${switchyard.version}">switchyard-rules</feature>
		<feature version="${switchyard.version}">switchyard-sca</feature>
		<feature version="${switchyard.version}">switchyard</feature>
		<bundle>mvn:${project.groupId}/${project.artifactId}/${project.version}</bundle>
	<feature version="${switchyard.version}">switchyard-bean</feature></feature>
</features>
